var compareFood = {
    //this css will be loaded automatically
   // css:['dashboard.css'],
    index: function ($scope, $location) {
        $scope.var1 = "Testing training";
        $scope.pageName = $location.path();
    }
};